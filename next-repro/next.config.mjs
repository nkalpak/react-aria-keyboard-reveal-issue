/** @type {import('next').NextConfig} */
const nextConfig = {
    // Only needed when opening the dev server from a phone over the LAN:
    // Next 16 blocks cross-origin /_next/* requests by default. Add your
    // machine's LAN IP here, e.g. allowedDevOrigins: ["192.168.1.50"].
    // allowedDevOrigins: [],
};

export default nextConfig;
